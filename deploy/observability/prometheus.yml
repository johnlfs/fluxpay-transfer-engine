global:
  scrape_interval: 5s
  scrape_timeout: 4s
  evaluation_interval: 5s

scrape_configs:
  - job_name: fluxpay-otel-collector

    honor_labels: true

    static_configs:
      - targets:
          - otel-collector:8889
